supported_versions: ">=0.13.0"
browser: chrome
concurrency: 4
timeout: 20
browser_timeout: 16
before_all:
  - command: npm i
  - command: cd jekyll_deps && bundle install
failure_screenshot_location: "_failure-screenshots"
