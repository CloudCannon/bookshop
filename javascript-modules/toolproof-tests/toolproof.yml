supported_versions: ">=0.15.0"
browser: chrome
concurrency: 4
timeout: 20
browser_timeout: 16
before_all:
  - command: pnpm install
  - command: cd jekyll_deps && bundle install
failure_screenshot_location: "_failure-screenshots"
